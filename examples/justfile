update:
  #!/usr/bin/env bash
  set -e
  while IFS= read -r yaml; do
    dir=$(dirname "$yaml")
    echo "Updating $dir..."
    d=$(realpath "$dir")
    (cd ../sqg && pnpm sqg "$d/sqg.yaml")
  done < <(find . -maxdepth 2 -name sqg.yaml -type f | sort)
  echo "All examples updated"

