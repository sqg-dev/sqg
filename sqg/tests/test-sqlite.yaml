version: 1

name: test-sqlite

sql:
  - files:
      - test-sqlite.sql
    gen:
        - generator: typescript/sqlite
          output: /tmp/
        - generator: java/sqlite
          output: /tmp/
          config:
            package: com.test.db


