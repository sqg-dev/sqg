version: 1

name: test-sqlite

sql:
  - files:
      - test-sqlite.sql
    gen:
        - generator: typescript/sqlite
          output: /tmp/
          config:
            migrations: true
        - generator: java/sqlite
          output: /tmp/
          config:
            package: com.test.db
            migrations: true


