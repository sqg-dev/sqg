---
title: SQG - Compile SQL to Type-Safe Code
description: Generate type-safe database code from SQL queries for TypeScript and Java
template: splash
hero:
  tagline: Write SQL. Get type-safe code. Ship faster.
  actions:
    - text: Get Started
      link: /guides/getting-started/
      icon: open-book
    - text: Try the Playground
      link: /playground/
      icon: rocket
      variant: primary
---

import { Card, CardGrid, LinkCard } from '@astrojs/starlight/components';

## SQL In, Type-Safe Code Out

Write your queries in plain SQL with simple annotations. SQG introspects your database at build time and generates fully typed code.

```sql
-- QUERY find_user :one
@set email = 'test@example.com'
SELECT * FROM users WHERE email = ${email};
```

becomes

```typescript
// TypeScript
findUser(email: string): User | undefined
```

```java
// Java
public UserRow findUser(String email) throws SQLException
```

No ORMs. No query builders. Just SQL and types.

<CardGrid stagger>
  <Card title="Real SQL" icon="pencil">
    Write standard SQL that runs directly against your database. No proprietary query language to learn.
  </Card>
  <Card title="True Type Safety" icon="approve-check">
    Types are introspected from your actual schema at build time. Catch errors before production.
  </Card>
  <Card title="Multiple Databases" icon="server">
    SQLite, DuckDB, and PostgreSQL. Same annotations work across all engines.
  </Card>
  <Card title="TypeScript & Java" icon="setting">
    Generate code for multiple languages from the same SQL definitions.
  </Card>
</CardGrid>

## How It Works

```d2 sketch pad=50
direction: right

Your SQL \n queries -> Database\n(introspect) -> Type-safe code\n(generated) {style.animated: false}
```

Most tools try to *parse* SQL to figure out types. SQG takes a simpler approach: **run the query**.

Your migrations set up the schema, SQG executes your queries against a real database, and captures the exact column names and types from the result. No parser limitations, no guessing—100% accurate types.

## Why Not an ORM?

| ORMs | SQG |
|------|-----|
| Learn a query builder API | Write standard SQL |
| Types generated from models | Types introspected from queries |
| Hidden SQL, hard to optimize | Transparent, debuggable |
| Runtime query translation | No runtime overhead |

SQG is for developers who want the safety of typed code without giving up control over their SQL.

## Generators

| Generator | Database | Language |
|-----------|----------|----------|
| [`typescript/sqlite`](/generators/typescript-sqlite/) | SQLite | TypeScript (sync) |
| [`typescript/duckdb`](/generators/typescript-duckdb/) | DuckDB | TypeScript (async) |
| [`java/sqlite`](/generators/java-jdbc/), [`java/duckdb`](/generators/java-jdbc/), [`java/postgres`](/generators/java-jdbc/) | SQLite, DuckDB, PostgreSQL | Java (JDBC) |
| [`java/duckdb/arrow`](/generators/java-duckdb-arrow/) | DuckDB | Java (Arrow API) |

## Get Started

<CardGrid>
  <LinkCard
    title="Quick Start"
    description="Install SQG and generate your first type-safe queries in minutes."
    href="/guides/getting-started/"
  />
  <LinkCard
    title="Playground"
    description="Try SQG in your browser—no installation required."
    href="/playground/"
  />
  <LinkCard
    title="SQL Syntax"
    description="Learn the annotation syntax for queries, migrations, and parameters."
    href="/guides/sql-syntax/"
  />
  <LinkCard
    title="Build with AI"
    description="Use SQG with Claude and Cursor to generate code through MCP."
    href="/guides/build-with-ai/"
  />
  <LinkCard
    title="FAQ"
    description="Common questions and troubleshooting."
    href="/guides/faq/"
  />
</CardGrid>

## Works with DBeaver

SQG's `@set` syntax is compatible with [DBeaver](https://dbeaver.io), the popular open-source database IDE. Develop and test your queries interactively, then run SQG to generate code.

## Open Source

SQG is open source under the Apache 2.0 license.

[GitHub](https://github.com/sqg-dev/sqg) · [Issues](https://github.com/sqg-dev/sqg/issues)
